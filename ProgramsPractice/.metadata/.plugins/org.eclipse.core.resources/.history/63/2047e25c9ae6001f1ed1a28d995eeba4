package interviews.practice;

import java.util.*;

public class UniqueIntegers {
	int a[]= {213,21,12,14,4,24,242,12,2,24,14};
	
	List<Integer> unique = Arrays.stream(a).distinct().collect(Collectors.toList());
	
}
